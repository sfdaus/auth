apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
  namespace: prakarsa
  labels: { app: auth-api }
spec:
  replicas: 1
  selector: { matchLabels: { app: auth-api } }
  template:
    metadata: { labels: { app: auth-api } }
    spec:
      containers:
        - name: auth-api
          image: auth-api:1.0   # image lokal yang kamu import ke k3s
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          # probe TCP biar aman kalau belum ada /healthz
          readinessProbe:
            tcpSocket: { port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket: { port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          # (opsional) ENV dari Secret .env
          envFrom:
            - secretRef: { name: auth-api-env }

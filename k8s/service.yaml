apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: prakarsa
  labels:
    app: auth
spec:
  type: ClusterIP
  selector:
    app: auth
  ports:
    - name: http
      port: 80
      targetPort: 8080